# non functional test - srtess testing,

# GIT
# backup tool
# deel conflicts - version control, we can do roll control
# Repository:
# folder - .git -
# commit - time & date, info (user and coment) (in .git)
# in cmd: mkdir - make folder, cd - go to that diraction, git init
# bash - we can get there in pycharm terminal
    # git config --global user.name "Gili Weinber"
    # git config --global user.email "gmmg33@gmail.com"
    # git init
    # git status
    # git add .
    # git commit -m "first commit"
    # git log